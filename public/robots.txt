# robots.txt for padelcourtsfinder.com
User-agent: *
Allow: /

# Block API routes
Disallow: /api/

# Block faceted/filtered search URLs — thin duplicate views that waste
# crawl budget. The real landing content lives on /[state]/[city] and
# /courts/ pages. The base /search tool stays crawlable.
Disallow: /search?

# Sitemap location
Sitemap: https://www.padelcourtsfinder.com/sitemap.xml
